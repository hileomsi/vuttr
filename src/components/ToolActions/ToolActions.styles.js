import styled from 'styled-components';
import { Input } from 'antd';

export const ToolActionsContainer = styled.div``;
export const Search = styled(Input.Search)`
  width: 150px;
`;
export const CheckboxContainer = styled.span`
  margin-left: 15px;
`;